apiVersion: v1
kind: Service
metadata:
  name: rook-ceph-dashboard-lb
  namespace: rook-ceph
  labels:
    app: rook-ceph-mgr
spec:
  type: LoadBalancer
  ports:
    - name: https-dashboard
      port: 8443
      targetPort: 8443
  selector:
    app: rook-ceph-mgr
